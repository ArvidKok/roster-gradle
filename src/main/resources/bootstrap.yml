spring:
  application:
    name: roster
  cloud:
    config:
      uri: ${vcap.services.cfg-srv-config.credentials.uri}
      
management:
  security:
    enabled: false

security:
  basic:
    enabled: false